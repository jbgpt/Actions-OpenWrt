#时区
cat << EOF > files/etc/config/system
config system
    option hostname 'OpenWrt'
    option zonename 'Asia/Shanghai'  # 时区设为上海
    option timezone 'CST-8'          # 中国标准时间
    option conloglevel '8'
    option cronloglevel '8'
EOF
